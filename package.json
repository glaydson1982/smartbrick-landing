{
  "name": "smartbrick-landing",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
